#!/usr/bin/env bash
set -e

# Check OS
if [ "$(uname -s)" != "Linux" ]
then
  exit
fi

export DOTFILES=$HOME/.dotfiles

# Install software
echo "› $DOTFILES/script/install"
$DOTFILES/script/install
